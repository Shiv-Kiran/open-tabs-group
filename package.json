{
  "name": "tabs-grouper",
  "private": true,
  "version": "0.1.1",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace apps/extension",
    "package": "npm run package --workspace apps/extension"
  }
}
